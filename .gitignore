node_modules
.env
.env.local
.next
out
dist
build
coverage
*.tsbuildinfo
next-env.d.ts
.vercel
.DS_Store
.claude
.cache

# Tesseract dépose son modèle dans le répertoire courant quand langPath est ignoré :
# le nôtre est versionné sous web/public/ocr, tout autre exemplaire est un résidu.
/fra.traineddata
web/fra.traineddata
.vitest

# Consignes générées par `next dev` pour les agents : régénérées à chaque
# lancement, elles n'ont pas leur place dans le dépôt.
web/AGENTS.md
web/CLAUDE.md
